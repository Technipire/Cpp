#include "journey_factory.h"

journey_factory::journey_factory() = default;

journey* journey_factory::output(std::string name)
{
	if (!journeyMap[name])
	{
		journeyMap[name] = journey->
	}
}